// Sidebar entries. Only routes that exist are listed; modules (tickets,
// departments, reports...) are added here as they are built, and permission
// filtering is layered on in Phase 6.
export const navigation = [{ label: 'Dashboard', to: '/dashboard' }]
